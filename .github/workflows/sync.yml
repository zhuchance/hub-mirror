name: Sync Images via Actions UI

on:
  workflow_dispatch:
    inputs:
      images:
        description: '请输入要同步的镜像（每行一个）'
        required: true
        type: string
        default: 'nginx:latest'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Aliyun ACR
        uses: docker/login-action@v3
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ALiyun_USER }}
          password: ${{ secrets.ALiyun_PWD }}

      - name: Process Images
        env:
          # 直接获取在 Actions 页面手动输入的文本
          RAW_BODY: ${{ inputs.images }}
        run: |
          printf "%s" "$RAW_BODY" > raw_images.txt

          while IFS= read -r line || [[ -n "$line" ]]; do
            CLEAN_IMG=$(echo "$line" | tr -d '\r' | tr -d '`' | xargs)
            
            [[ -z "$CLEAN_IMG" ]] && continue
            [[ "$CLEAN_IMG" == *" "* ]] && continue

            echo "--------------------------------"
            echo "正在同步: [$CLEAN_IMG]"
            
            IMAGE_NAME_TAG=$(echo "$CLEAN_IMG" | awk -F'/' '{print $NF}')
            TARGET="registry.cn-hangzhou.aliyuncs.com/mgfw/${IMAGE_NAME_TAG}"
            
            if docker pull "$CLEAN_IMG" < /dev/null; then
              docker tag "$CLEAN_IMG" "$TARGET"
              docker push "$TARGET" < /dev/null
              echo "✅ 成功: $TARGET"
            else
              echo "❌ 失败: $CLEAN_IMG"
            fi
          done < raw_images.txt
